# 合并开票 — 情境推演表

> 目的：说明「合并成一行」什么时候安全、什么时候会算错钱。
> 一句话结论：**合并「算好的结果」安全；把 FOC / min charge / 阶梯价套在「合并后的总数」上，会算错钱。**

---

## 情境 A — RENTAL（32 台同型号）

条件：32 台，同一个 rental meter type，每台 **908.20**，租金没有 FOC、没有用量、没有阶梯价。

| 做法 | 发票长什么样 | 金额 |
|---|---|---|
| 现在（一台一行） | 32 行 × 908.20 | **29,062.40** |
| 合并 | `RA-32 UNIT ... 32 UNIT × 908.20` | **29,062.40** |

✅ **完全一样。租金合并没有任何风险** —— 因为每台的金额本来就相同。

---

## 情境 B — METER（BK 抄表）：问题在这里

条件：4 台机器（少一点好看），同一个 meter type，单价 **0.03**，每台 FOC **100 张**。

### B-1 每台各自算（现在的做法 = 正确答案）

| 机器 | 上次 | 本次 | 用量 | FOC | 实收张数 | 金额 |
|---|---|---|---|---|---|---|
| A | 1,000 | 1,500 | 500 | 100 | 400 | 12.00 |
| B | 2,000 | 2,600 | 600 | 100 | 500 | 15.00 |
| C | 500 | 550 | **50** | 100 | **0** ← 不够扣，归零 | 0.00 |
| D | 3,000 | 3,800 | 800 | 100 | 700 | 21.00 |
| **合计** | | | **1,950** | 400 | **1,600** | **48.00** ✅ |

### B-2 如果「合并后才扣 FOC」

| 做法 | 算式 | 金额 | 差 |
|---|---|---|---|
| 总用量 1,950，**FOC 只给一份 100** | (1950 − 100) × 0.03 | **55.50** | 🔴 **多收 7.50（+15.6%）** |
| 总用量 1,950，**FOC 给 4 份 400** | (1950 − 400) × 0.03 | **46.50** | 🔴 **少收 1.50** |

第二个为什么会少收？**C 只用了 50 张，但有 100 张免费额度** —— 那 50 张本来是「浪费掉」的，合并之后被别台机器用掉了。

---

## ✅ 正确的合并方式

**先每台扣自己的 FOC，再把「实收张数」加起来当 Qty。**

| | 错的 | 对的 |
|---|---|---|
| Qty 用什么 | Σ **用量** = 1,950 | Σ **实收张数** = 1,600 |
| FOC 什么时候扣 | 合并**之后**扣一次 | 合并**之前**每台各扣 |
| 发票行 | 1,950 × 0.03 = 55.50 ❌ | **1,600 × 0.03 = 48.00** ✅ |

**发票上一样是一行，金额一分不差。** 这就是「合并显示、不合并计算」。

---

## 情境 C — Minimum Charge（最低消费）

同样 4 台，每台最低消费 **RM 50**。

| 机器 | 用量金额 | 每台底 50 | 实收 |
|---|---|---|---|
| A | 12.00 | ← 底 | **50.00** |
| B | 15.00 | ← 底 | **50.00** |
| C | 0.00 | ← 底 | **50.00** |
| D | 21.00 | ← 底 | **50.00** |
| **合计** | 48.00 | | **200.00** ✅ |

合并后只有**一个底**：`max(48.00, 50.00)` = **50.00**

🔴 **少收 150.00（−75%）**

而且 **「把底乘 4 = 200」也救不回来** —— 总额一旦超过 200，每一台的底就永远失效了。

> **最低消费必须是每台一个底，否则它就不叫最低消费。**

---

## 情境 D — 阶梯价（Multi-price）

阶梯：**0～1,000 张免费，超过 0.027**。4 台，每台用 3,000 张。

| 做法 | 算式 | 金额 | 差 |
|---|---|---|---|
| 每台各算 | (3,000 − 1,000) × 0.027 × 4 台 | **216.00** ✅ | |
| 合并后套阶梯 | (12,000 − 1,000) × 0.027 | **297.00** | 🔴 **多收 81.00（+37.5%）** |

原因：**4 份免费额度变成 1 份。**

⚠️ 而且方向不固定 —— 换一种阶梯（有量大折扣的），合并后会变成**少收 50%**。多收还是少收，取决于阶梯长什么样，**不是一个可以预估的偏差**。

---

## 总表

| 项目 | 合并计算安全吗 | 后果 |
|---|---|---|
| **Rental**（每台同价） | ✅ 安全 | 金额完全相同 |
| **Rebate %** | ✅ 安全 | 线性，32 行误差 ≤ RM 0.16 |
| **FOC** | ❌ 不安全 | 一份 → 多收；乘 N → 少收（浪费的额度被挪用） |
| **Min charge** | ❌ 最严重 | 最多 **−94%**，且乘 N 也救不回来 |
| **阶梯价** | ❌ 不安全 | **±50%，方向不定** |
| **FOC reset 周期** | ❌ 无解 | 混合周期的机队，选哪个都错 |

---

## 一个现实的提醒

情境 B 里 4 台机器的金额是 12 / 15 / 0 / 21 —— **全部不一样**。

所以 meter 行要合并成一行，**Qty 必须是「总实收张数」、单价必须是「共同单价」**，而且只有在以下条件全部成立时才合得起来：

- 每台的**有效单价相同**（同费率、同阶梯）
- **没有任何一台触到最低消费**（触底的那台单价已经不是费率了）
- 同 rebate %、同 FOC reset 周期、同合同

只要有一台触底或用了不同阶梯，**那一台就要自己单独一行** —— 否则那一行的 `Qty × 单价` 就不等于实收金额。

---

## 结论

> **合并是「把算好的结果并起来显示」，不是「把原料倒在一起再算」。**

发票外观 100% 是要的样子；钱一分不动；每台机器的抄表记录、Appendix A 清单、CN 更正全部照旧一台一笔。
